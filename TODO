- Suppressing INT1 when switching power off (CMD_PWR_SWITCH) doesn't work.
- Wrappers or macros to EZ-USB specific operations, which are not covered by fx2lib
  (sth like fx2lib extension).

- Move dscr.a51 contents to C file

- Test CMD_FPGA_READ_REGS

- MBM27C interface requires VPP=21V, so support for an adapter with DC/DC converter should be added

- Test CMD_DRIVER_READ_ID
