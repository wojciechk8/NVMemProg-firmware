- Move dscr.a51 contents to C file

- Test CMD_FPGA_READ_REGS

- MBM27C interface requires VPP=21V, so support for an adapter with DC/DC converter should be added

- Test CMD_DRIVER_READ_ID
- Test CMD_DRIVER_WRITE_ID
- Test new driver handling (CMD_DRIVER_RESET)

- add ifc_ready to status (replace with busy)

- report error count (ERRCNTLIM) in device status

- no need to switch between manual and auto FIFO mode (could remain in auto)
